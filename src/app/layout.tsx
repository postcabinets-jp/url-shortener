import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "snip.link — Free URL Shortener with Custom Domains",
  description:
    "Open-source Bitly alternative. Unlimited links, free custom domains, real-time analytics, QR codes, team management. Self-host on Vercel + Supabase in 5 minutes.",
  openGraph: {
    title: "snip.link — Free URL Shortener with Custom Domains",
    description: "Open-source Bitly alternative with unlimited links, free custom domains, and real-time analytics.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Toaster position="bottom-right" />
      </body>
    </html>
  );
}
